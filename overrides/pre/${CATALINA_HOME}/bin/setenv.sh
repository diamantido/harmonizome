#!/bin/sh

export ORIGIN="${ORIGIN}"
export HARMONIZOME_PREFIX="${HARMONIZOME_PREFIX}"
export HARMONIZOME_ML_URL="${HARMONIZOME_ML_URL}"
export CLUSTERGRAMMER_URL="${CLUSTERGRAMMER_URL}"
export ENRICHR_URL="${ENRICHR_URL}"
export ARCHS4_URL="${ARCHS4_URL}"
export HARMONOGRAM_URL="${HARMONOGRAM_URL}"
export STATIC_URL="${STATIC_URL}"
export DATA_DIR="${DATA_DIR}"
export ML_DATA_DIR="${ML_DATA_DIR}"
export HEAT_MAP_IMAGES_DIR="${HEAT_MAP_IMAGES_DIR}"
export DB_URL="${DB_URL}"
export DB_USER="${DB_USER}"
export DB_PASS="${DB_PASS}"
export SMTP_HOST="${SMTP_HOST}"
export SMTP_PORT="${SMTP_PORT}"
export FROM_EMAIL="${FROM_EMAIL}"
export FROM_USER="${FROM_USER}"
export FROM_PASS="${FROM_PASS}"
export EMAIL_TO="${EMAIL_TO}"
export EMAIL_CC="${EMAIL_CC}"
